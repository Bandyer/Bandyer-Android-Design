<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/smartglass_nav_graph_chat"
    app:startDestination="@id/chatFragment">

    <fragment
        android:id="@+id/chatFragment"
        android:name="com.bandyer.app_design.smartglass.ChatFragment"
        android:label="ChatFragment">
        <action
            android:id="@+id/action_chatFragment_to_chatMenuFragment"
            app:destination="@id/chatMenuFragment" />
    </fragment>
    <fragment
        android:id="@+id/chatMenuFragment"
        android:name="com.bandyer.app_design.smartglass.ChatMenuFragment"
        android:label="ChatMenuFragment">
        <argument
            android:name="contactData"
            android:defaultValue="@null"
            app:argType="com.bandyer.video_android_glass_ui.contact.BandyerContactData"
            app:nullable="true" />
    </fragment>
</navigation>